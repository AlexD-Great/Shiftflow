{
  "name": "@shiftflow/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for ShiftFlow - Conditional execution layer for cross-chain DeFi",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "lint": "eslint src"
  },
  "keywords": [
    "defi",
    "cross-chain",
    "automation",
    "sideshift",
    "workflow"
  ],
  "dependencies": {
    "@shiftflow/engine": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}
